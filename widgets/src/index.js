// importing React, ReactDom and the App component
import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';

// rendering the App component in the root
ReactDOM.render(<App />, document.querySelector('#root'));
